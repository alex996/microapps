import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#29045e',
      dark: '#130833'
    },
    secondary: {
      main: '#ff007d'
    },
    tertiary: {
      main: '#1ba0ff'
    }
  }
})

export default theme
